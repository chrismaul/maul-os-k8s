[Unit]
Description=kubernetes directory Directory
Before=kubelet.service

[Mount]
What=/dev/disk/by-label/k8s
Where=/etc/cni
Type=btrfs
Options=subvol=_etc-cni,defaults,relatime

[Install]
WantedBy=kubelet.service
